'use strict';

module.exports = /*@ngInject*/
    function directiveFour($rootScope) {
        return {
        	templateUrl: 'common/directives/directiveFour.html',
            link: function (scope, element) {
            	//globalFunction();
            }
        };
    };
